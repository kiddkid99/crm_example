@model CRM.Models.Supply

@{
    ViewBag.Title = "刪除";
}

<h2>@ViewBag.Title</h2>

<h3>您確定要刪除這筆資料嗎？</h3>
<div>
    <h4>商品類別</h4>
    <hr />

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SupplyName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SupplyName)
        </dd>

    </dl>

    @using (Ajax.BeginForm(new AjaxOptions { UpdateTargetId = "panel", HttpMethod = "post" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="刪除" class="btn btn-default" /> |
            @Ajax.ActionLink("回上一頁", "Index", new AjaxOptions { UpdateTargetId = "panel" })
        </div>
    }
</div>
